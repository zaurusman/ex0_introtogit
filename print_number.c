#include <stdio.h>

int main() {
        int course_number[6] = {2, 3, 4, 1, 2, 4};
        for(int i = 0; i < 6; i++) {
            printf("%d", course_number[i]);
        }
        printf("\n");
        return 0;
}
